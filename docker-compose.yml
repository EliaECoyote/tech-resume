version: "3.7"

services:
  bs-node:
    image: gcr.io/tech-resume/bs-node
    build:
      context: .
      dockerfile: Dockerfile.bs-node
      args:
        BS_PLATFORM_VERSION: 7.0.1
  web:
    env_file:
      - .env
    command: sh ./scripts/startup.sh
    container_name: web
    build:
      context: ./web
      target: builder
      args:
        - GATSBY_API_SCHEME=http
        - GATSBY_API_HOST=127.0.0.1:3000
    volumes:
      - ./web/src:/usr/node/app/src:cached
      - ./web/scripts:/usr/node/app/scripts:cached
      - ./web/package.json:/usr/node/app/package.json:cached
      - ./web/yarn.lock:/usr/node/app/yarn.lock:cached
      - ./web/bsconfig.json:/usr/node/app/bsconfig.json:cached
      - ./web/gatsby-browser.js:/usr/node/app/gatsby-browser.js:cached
      - ./web/gatsby-config.js:/usr/node/app/gatsby-config.js:cached
      - ./web/gatsby-node.js:/usr/node/app/gatsby-node.js:cached
      - ./web/gatsby-ssr.js:/usr/node/app/gatsby-ssr.js:cached
      - ./web/jest.config.js:/usr/node/app/jest.config.js:cached
      - ./web/babel.config.js:/usr/node/app/babel.config.js:cached
    ports:
      - "8000:8000"
  api:
    environment:
      - SCHEME=http
      - HOST=127.0.0.1
      - PORT=3000
    env_file:
      - .env
    command: sh ./scripts/startup.sh
    container_name: api
    build:
      context: ./api
    volumes:
      - ./api/src:/usr/node/app/src:cached
      - ./api/scripts:/usr/node/app/scripts:cached
      - ./api/package.json:/usr/node/app/package.json:cached
      - ./api/yarn.lock:/usr/node/app/yarn.lock:cached
      - ./api/nodemon.json:/usr/node/app/nodemon.json:cached
      - ./api/bsconfig.json:/usr/node/app/bsconfig.json:cached
    ports:
      - "3000:3000"
      - "3001:3001"
  pdfgen:
    privileged: true
    env_file:
      - .env
    command: sh ./scripts/startup.sh
    build:
      context: ./pdfgen
    container_name: pdfgen
    volumes:
      - ./pdfgen/src:/usr/node/app/src:cached
      - ./pdfgen/scripts:/usr/node/app/scripts:cached
      - ./pdfgen/package.json:/usr/node/app/package.json:cached
      - ./pdfgen/yarn.lock:/usr/node/app/yarn.lock:cached
      - ./pdfgen/nodemon.json:/usr/node/app/nodemon.json:cached
      - ./pdfgen/bsconfig.json:/usr/node/app/bsconfig.json:cached
    ports:
      - "4000:4000"
      - "4001:4001"
